services:
  - type: web
    name: employee-feedback-agent
    env: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        value: sqlite:////app/feedback_agent.db
